---
phase: 03-react-draft-board
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/ChampionGrid.tsx
  - frontend/src/components/ChampionIcon.tsx
  - frontend/src/components/DraftBoard.tsx
  - frontend/src/components/TeamPanel.tsx
  - frontend/src/components/BanRow.tsx
  - frontend/src/components/PickSlot.tsx
  - frontend/src/components/TeamSelector.tsx
  - frontend/src/components/WinProbability.tsx
  - frontend/src/components/ModeToggle.tsx
  - frontend/src/components/DraftControls.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
  - DRAFT-04
  - DRAFT-07

must_haves:
  truths:
    - "All ~167 champion icons render in a scrollable grid using DDragon CDN images"
    - "Typing in search bar filters champions in real-time"
    - "Draft board shows blue team on left, red team on right, with 5 ban slots and 5 pick slots each"
    - "Team selector dropdown shows teams grouped by LCK, LEC, LCS, LPL"
    - "Mode toggle shows Live Draft and Quick Entry tabs"
    - "Win probability bar is centered between team panels"
  artifacts:
    - path: "frontend/src/components/ChampionGrid.tsx"
      provides: "Searchable champion icon grid consuming useChampions() hook"
      min_lines: 30
    - path: "frontend/src/components/DraftBoard.tsx"
      provides: "Main two-sided layout container"
      min_lines: 30
    - path: "frontend/src/components/TeamPanel.tsx"
      provides: "One side: bans row + pick slots + team selector"
      min_lines: 40
    - path: "frontend/src/components/TeamSelector.tsx"
      provides: "League-grouped team dropdown consuming useTeams() hook"
      min_lines: 20
    - path: "frontend/src/components/WinProbability.tsx"
      provides: "Horizontal split bar with blue/red percentages"
      min_lines: 15
  key_links:
    - from: "frontend/src/components/ChampionGrid.tsx"
      to: "useChampions()"
      via: "TanStack Query hook for fetching champion list"
      pattern: "useChampions"
    - from: "frontend/src/components/ChampionIcon.tsx"
      to: "DDragon CDN"
      via: "img src from champion.image_url"
      pattern: "image_url"
    - from: "frontend/src/components/TeamSelector.tsx"
      to: "useTeams()"
      via: "TanStack Query hook for fetching team list"
      pattern: "useTeams"
    - from: "frontend/src/App.tsx"
      to: "DraftBoard + ChampionGrid"
      via: "Main layout composition"
      pattern: "DraftBoard"
---

<objective>
Build all visual UI components: champion grid with search, two-sided draft board with ban/pick slots, team selectors, mode toggle, win probability bar, and draft controls. Wire them into App.tsx to produce the complete draft board layout.

Purpose: This plan creates the visible UI shell that users interact with. All subsequent plans (live mode, bulk mode, series) layer behavior onto these components.
Output: A fully rendered draft board page with champion grid, team panels, and all visual elements in the LoL dark theme.
</objective>

<execution_context>
@/home/lvc/.claude/get-shit-done/workflows/execute-plan.md
@/home/lvc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-draft-board/03-RESEARCH.md
@.planning/phases/03-react-draft-board/03-CONTEXT.md
@.planning/phases/03-react-draft-board/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ChampionGrid, ChampionIcon, and search functionality</name>
  <files>
    frontend/src/components/ChampionGrid.tsx
    frontend/src/components/ChampionIcon.tsx
  </files>
  <action>
Create src/components/ChampionIcon.tsx:
- Props: `champion: ChampionInfo`, `disabled: boolean`, `onClick: () => void`
- Render a 48x48 DDragon portrait image from `champion.image_url` with `loading="lazy"`
- Use `React.memo` for performance (167 icons in grid)
- When disabled (already picked/banned): reduce opacity to ~30%, change cursor to not-allowed, ignore clicks
- Add `onError` handler on img that sets a fallback placeholder (gray square with "?" text)
- On hover (when not disabled): slight brightness increase and a subtle border glow (blue-team or gold color)
- Dark panel background for each icon cell

Create src/components/ChampionGrid.tsx:
- Props: `onSelect: (championName: string) => void`
- Uses `useChampions()` hook to fetch champion data
- Internal state: `searchQuery` string (controlled input)
- Renders search bar at top: text input with placeholder "Search champions...", styled with dark panel background, light text, gold focus border
- Filters champions by `name.toLowerCase().includes(searchQuery.toLowerCase())`
- Gets `usedChampions` from Zustand store via selector (stable Set reference)
- Renders filtered champions in a CSS Grid: `grid-cols-10` on large screens (responsive: `grid-cols-8` on medium, `grid-cols-6` on smaller)
- Gap of 1-2px between icons
- Shows loading skeleton grid (10x5 gray rectangles) while `isPending`
- Already picked/banned champions: pass `disabled={usedChampions.has(champ.name)}` to ChampionIcon
- Grid is in a scrollable container with max-height so it does not push the draft board off-screen
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Start Vite dev server and FastAPI backend; champion grid should show ~167 DDragon portraits with working search filter</manual>
  </verify>
  <done>Champion grid renders all champions from DDragon CDN, search filters in real-time, disabled champions are visually dimmed</done>
</task>

<task type="auto">
  <name>Task 2: Build draft board layout with team panels, slots, selectors, mode toggle, and controls</name>
  <files>
    frontend/src/components/DraftBoard.tsx
    frontend/src/components/TeamPanel.tsx
    frontend/src/components/BanRow.tsx
    frontend/src/components/PickSlot.tsx
    frontend/src/components/TeamSelector.tsx
    frontend/src/components/WinProbability.tsx
    frontend/src/components/ModeToggle.tsx
    frontend/src/components/DraftControls.tsx
    frontend/src/App.tsx
  </files>
  <action>
Create src/components/BanRow.tsx:
- Props: `bans: (string | null)[]` (length 5), `side: Side`, `activeBanIndex: number | null`
- Renders 5 small ban slots in a horizontal row (32x32 each)
- Filled slots show the champion portrait (small) with a red X overlay or desaturated tint
- Empty slots show a dark placeholder with ban icon outline
- Active slot (currently being filled) has a pulsing border (blue-team or red-team color)

Create src/components/PickSlot.tsx:
- Props: `champion: string | null`, `role: Role | null`, `side: Side`, `slotIndex: number`, `isActive: boolean`, `onClick: () => void`
- Larger slot (64x64 or 72x72) for picked champions
- Filled: shows champion portrait, role label below
- Empty: dark placeholder with slot number or role icon
- Active: highlighted border (blue-team or red-team color)
- Clicking an empty slot in bulk mode makes it the active slot

Create src/components/TeamSelector.tsx:
- Props: `side: Side`, `selectedTeam: string | null`, `onSelect: (teamName: string) => void`
- Uses `useTeams()` hook
- Renders a styled `<select>` with `<optgroup>` per league (LCK, LEC, LCS, LPL, Other)
- Default option: "Select Team"
- Styled with dark panel background, side-colored border (blue-team or red-team)

Create src/components/TeamPanel.tsx:
- Props: `side: Side`
- Reads from Zustand store: bans, picks, team, roles for this side
- Reads current active slot info from store to highlight the correct ban/pick slot
- Layout (top to bottom): Team name/selector, BanRow (5 bans), PickSlots (5 picks arranged vertically)
- Blue panel has subtle blue-team left border, Red panel has red-team right border
- Panel background: panel color (#1a1a2e)

Create src/components/WinProbability.tsx:
- Props: `blue: number | null`, `red: number | null`, `isPending: boolean`
- When both are null (no prediction yet): shows "Complete draft to predict" placeholder text
- When isPending: shows loading spinner/pulse animation
- When populated: horizontal split bar with blue section (blue-team color) and red section (red-team color), percentage text inside each section, rounded edges
- Bar height ~40px, full width of its container

Create src/components/ModeToggle.tsx:
- Props: none (reads/writes mode from Zustand store directly)
- Two tabs: "Live Draft" and "Quick Entry"
- Active tab has gold underline/highlight, inactive tab is dimmed
- Clicking switches mode via `store.setMode()`

Create src/components/DraftControls.tsx:
- Props: none (reads from store, calls store actions)
- Contains: "Get Prediction" button (disabled until `isDraftReady()`), "Reset Draft" button, "Reset All" button
- Get Prediction button: gold/accent color, prominent
- Reset buttons: subtle/secondary styling
- For now, Get Prediction just calls `usePrediction().mutate()` with a `buildPredictRequest()` helper -- wire the prediction flow (build the request from store state, submit, display result in WinProbability)

Create src/components/DraftBoard.tsx:
- Main container with the overall layout per CONTEXT.md decisions:
  - ModeToggle at the top
  - Three-column layout in the middle: TeamPanel(blue) | WinProbability | TeamPanel(red)
  - Use CSS Grid or Flexbox for the three-column arrangement
- Passes prediction state (from usePrediction hook) to WinProbability

Update src/App.tsx:
- Replace placeholder with full layout: DraftBoard at top, ChampionGrid below
- ChampionGrid's `onSelect` calls `store.selectChampion(name)`
- Overall page: `bg-background text-text-primary min-h-screen` with appropriate padding
- Add a header with "LoL Draft Predictor" title in gold-light color, subtitle "Match Outcome Prediction" in text-secondary
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>Visit localhost:5173 with backend running -- should see two-sided draft board with empty slots, team dropdowns, mode toggle, champion grid below, and the full dark LoL theme</manual>
  </verify>
  <done>Complete draft board layout renders with blue/red team panels, 5 ban + 5 pick slots each, team selectors with league groups, mode toggle tabs, win probability placeholder, and champion grid with search</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds
2. `cd frontend && npx tsc --noEmit` passes
3. Page renders champion grid with DDragon images (requires backend running for /api/champions)
4. Draft board shows two-sided layout with empty slots
5. Team selector populates from API with league groupings
6. Search input filters champion grid in real-time
</verification>

<success_criteria>
- All ~167 champion portraits load from DDragon CDN in a scrollable grid
- Search bar filters champions by name in real-time
- Two-sided draft board with blue left / red right, 5 bans + 5 picks per side
- Team selectors show teams grouped by league
- Mode toggle shows Live Draft / Quick Entry tabs
- Win probability bar placeholder centered between panels
- Full LoL dark theme with correct accent colors
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-draft-board/03-02-SUMMARY.md`
</output>
