---
phase: 03-react-draft-board
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/src/store/draftStore.ts
  - frontend/src/components/DraftBoard.tsx
  - frontend/src/components/DraftControls.tsx
  - frontend/src/components/PickSlot.tsx
  - frontend/src/components/RoleAssignment.tsx
  - frontend/src/components/WinProbability.tsx
  - frontend/src/components/BanRow.tsx
autonomous: true
requirements:
  - BULK-01
  - BULK-02
  - DRAFT-05
  - DRAFT-06

must_haves:
  truths:
    - "In bulk mode, user can click any empty slot to make it active, then click a champion to fill it"
    - "All 20 slots are fillable in any order in bulk mode"
    - "After all 5 picks per team, role assignment UI appears for that team"
    - "User can assign Top/Jungle/Mid/Bot/Support to each picked champion"
    - "Get Prediction button submits the draft and displays win probability split bar"
    - "Win probability bar shows blue and red percentages after successful prediction"
  artifacts:
    - path: "frontend/src/components/RoleAssignment.tsx"
      provides: "Post-pick role assignment UI with dropdowns per champion"
      min_lines: 40
    - path: "frontend/src/components/WinProbability.tsx"
      provides: "Split bar displaying blue/red win percentages"
      min_lines: 20
    - path: "frontend/src/components/DraftControls.tsx"
      provides: "Get Prediction button wired to usePrediction mutation"
      min_lines: 25
  key_links:
    - from: "frontend/src/components/DraftControls.tsx"
      to: "POST /api/predict"
      via: "usePrediction() mutation with buildPredictRequest()"
      pattern: "usePrediction"
    - from: "frontend/src/store/draftStore.ts"
      to: "activeSlot"
      via: "Bulk mode slot selection"
      pattern: "activeSlot"
    - from: "frontend/src/components/RoleAssignment.tsx"
      to: "frontend/src/store/draftStore.ts"
      via: "setRole(side, role, champion)"
      pattern: "setRole"
---

<objective>
Implement bulk entry mode (click any slot in any order), role assignment UI (assign roles after picks are complete), and the prediction submission flow with win probability display.

Purpose: Bulk mode provides a faster alternative to live draft for post-game analysis. Role assignment and prediction display are the final steps of both modes -- the visual payoff of the entire application.
Output: Users can fill all 20 slots in any order, assign roles, submit for prediction, and see the win probability split bar.
</objective>

<execution_context>
@/home/lvc/.claude/get-shit-done/workflows/execute-plan.md
@/home/lvc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-draft-board/03-RESEARCH.md
@.planning/phases/03-react-draft-board/03-01-SUMMARY.md
@.planning/phases/03-react-draft-board/03-02-SUMMARY.md
@api/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bulk entry mode with arbitrary slot selection</name>
  <files>
    frontend/src/store/draftStore.ts
    frontend/src/components/PickSlot.tsx
    frontend/src/components/BanRow.tsx
    frontend/src/components/DraftBoard.tsx
  </files>
  <action>
Update store/draftStore.ts for bulk mode:
- In bulk mode, `selectChampion(name)` places into the slot defined by `activeSlot` state (not `currentStep`)
- `setActiveSlot(team, action, index)` sets which slot will receive the next champion
- After placing a champion in bulk mode, auto-advance `activeSlot` to the next empty slot (scanning left-to-right, bans then picks, blue then red) -- or set to null if no empty slots remain
- `setMode('bulk')` preserves all current picks/bans. `setMode('live')` recalculates `currentStep` by scanning DRAFT_SEQUENCE to find the first step whose slot is empty.

Update BanRow.tsx:
- In bulk mode, each empty ban slot is clickable. Clicking calls `store.setActiveSlot(side, 'ban', index)`.
- Active slot (matching activeSlot) has the pulsing highlight.
- Filled slots are not clickable.

Update PickSlot.tsx:
- In bulk mode, each empty pick slot is clickable. Clicking calls `store.setActiveSlot(side, 'pick', index)`.
- Active slot has the glowing highlight.
- Filled slots are not clickable.

Update DraftBoard.tsx:
- In bulk mode, the phase label changes to show "Quick Entry -- Click a slot then select a champion" (or similar guidance text)
- Show a count of filled slots: "8/20 selections made"
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>Switch to Quick Entry mode. Click a ban slot on the red team -- it highlights. Click a champion -- it fills that slot. Click a pick slot on blue team -- it highlights. Fill slots in any order. Verify all 20 slots can be filled.</manual>
  </verify>
  <done>Bulk mode allows clicking any empty slot to make it active, then clicking a champion to fill it, with auto-advance to next empty slot</done>
</task>

<task type="auto">
  <name>Task 2: Build role assignment UI, prediction submission, and win probability display</name>
  <files>
    frontend/src/components/RoleAssignment.tsx
    frontend/src/components/DraftControls.tsx
    frontend/src/components/WinProbability.tsx
    frontend/src/components/DraftBoard.tsx
    frontend/src/store/draftStore.ts
  </files>
  <action>
Create src/components/RoleAssignment.tsx:
- Props: `side: Side`
- Reads picks and roles from Zustand store for this side
- Only renders when all 5 picks for this side are filled (check `picks.every(p => p !== null)`)
- Shows 5 rows, one per picked champion: champion portrait (small, 32x32), champion name, and a `<select>` dropdown with roles (Top, Jungle, Mid, Bot, Support)
- When a role is selected, calls `store.setRole(side, role, championName)`
- Prevent duplicate role assignment: already-assigned roles are disabled in other dropdowns
- Visual: panel-light background, side-colored header ("Blue Team Roles" / "Red Team Roles")
- Show a checkmark or green indicator when all 5 roles are assigned

Update store/draftStore.ts:
- Add `buildPredictRequest()` method or export a standalone function that constructs a `PredictRequest` from current store state. Maps `blueRoles` (Record<Role, string | null>) to the API's `TeamDraft` shape: `{ top: blueRoles.top!, jungle: blueRoles.jungle!, ... }`. Same for red.
- `isDraftReady()` checks: all 20 slots filled, both teams selected, all 10 roles assigned (no nulls in blueRoles/redRoles)

Update DraftControls.tsx:
- "Get Prediction" button: uses `usePrediction()` mutation. On click, calls `buildPredictRequest()` from store state and passes to `mutation.mutate()`. Disabled when `!isDraftReady()`.
- Show `mutation.isPending` as loading state on button (spinner or "Predicting..." text)
- Show `mutation.isError` as error message below button (red text with `mutation.error.message`)
- "Reset Draft" calls `store.resetDraft()` and `mutation.reset()`
- "Reset All" calls `store.resetAll()` and `mutation.reset()`

Update WinProbability.tsx:
- Receive prediction data from DraftBoard (passed as props from usePrediction result)
- When `data` is populated: render the horizontal split bar
  - Blue section: `bg-blue-team`, width proportional to `blue_win_probability`, shows percentage text (e.g., "56.3%")
  - Red section: `bg-red-team`, width proportional to `red_win_probability`, shows percentage text
  - Bar has rounded corners, height ~40px
  - Percentage text is white, bold, centered in each section
- When no prediction: show muted text "Complete the draft to see prediction"
- When loading: show a pulsing/skeleton bar

Update DraftBoard.tsx:
- Add RoleAssignment components for each side, positioned below the pick slots in each TeamPanel or in a dedicated section after the draft board
- Pass usePrediction() result data to WinProbability
- Role assignment appears per-team as soon as that team's 5 picks are filled (per RESEARCH.md recommendation), regardless of mode
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>Complete a full draft (either mode), select teams, assign all roles, click "Get Prediction" with backend running. Verify win probability bar appears with blue/red percentages. Test error state by stopping the backend and clicking predict.</manual>
  </verify>
  <done>Role assignment UI appears after 5 picks per team, prediction submits correctly to API, win probability split bar displays blue/red percentages</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds
2. Bulk mode: all 20 slots fillable in arbitrary order
3. Role assignment appears per-team after 5 picks
4. Role dropdowns prevent duplicate assignment
5. Get Prediction button disabled until draft is fully ready
6. Prediction displays correctly as blue/red split bar with percentages
7. Error states handled (API down, validation errors)
</verification>

<success_criteria>
- Bulk mode slot selection works in any order
- Role assignment UI maps all 5 champions to roles per team
- Prediction submission constructs correct PredictRequest matching API schema
- Win probability bar renders with accurate percentages from API response
- Loading and error states handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-draft-board/03-04-SUMMARY.md`
</output>
