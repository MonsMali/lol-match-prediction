---
phase: 04-integration-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/main.py
  - frontend/dist/
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "FastAPI serves the compiled React SPA at the root URL"
    - "API routes (/api/*, /health) still respond correctly when SPA catch-all is active"
    - "Production requirements file excludes XGBoost, LightGBM, CatBoost, Optuna, and pandas"
  artifacts:
    - path: "api/main.py"
      provides: "SPA catch-all route serving frontend/dist/index.html"
      contains: "frontend.*dist"
    - path: "frontend/dist/index.html"
      provides: "Compiled React SPA entry point"
    - path: "requirements-prod.txt"
      provides: "Production-only Python dependencies"
      contains: "scikit-learn==1.5.0"
  key_links:
    - from: "api/main.py"
      to: "frontend/dist/"
      via: "FileResponse serving SPA assets and index.html fallback"
      pattern: "FileResponse.*index\\.html"
    - from: "api/main.py"
      to: "api/routers/*"
      via: "API routers registered BEFORE SPA catch-all"
      pattern: "include_router.*spa"
---

<objective>
Build the React SPA and configure FastAPI to serve it as static files, creating a single-process full-stack application.

Purpose: DEPLOY-02 requires the frontend SPA to be built and served by FastAPI as static files in a single deployment. DEPLOY-03 requires the production requirements to exclude training-only dependencies. This plan creates the integration layer between the Phase 2 backend and Phase 3 frontend.

Output: A working full-stack app where `uvicorn api.main:app` serves both the API and the React SPA from compiled static files.
</objective>

<execution_context>
@/home/lvc/.claude/get-shit-done/workflows/execute-plan.md
@/home/lvc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-deployment/04-RESEARCH.md
@.planning/phases/02-fastapi-backend/02-01-SUMMARY.md
@.planning/phases/03-react-draft-board/03-01-SUMMARY.md
@api/main.py
@frontend/package.json
@frontend/vite.config.ts
@requirements-prod.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build React SPA and add SPA catch-all to FastAPI</name>
  <files>api/main.py, frontend/dist/</files>
  <action>
1. Build the frontend SPA:
   - cd frontend && npm ci && npm run build
   - This produces frontend/dist/ with index.html and assets/

2. Modify api/main.py to serve the SPA. Add these imports at the top:
   ```python
   from pathlib import Path
   from fastapi.responses import FileResponse
   ```

3. After ALL existing router includes (health, predict, champions, teams, admin), add the SPA catch-all:
   ```python
   # ---------------------------------------------------------------------------
   # SPA static file serving (MUST be AFTER all API routers)
   # ---------------------------------------------------------------------------
   DIST_DIR = Path(__file__).resolve().parent.parent / "frontend" / "dist"

   if DIST_DIR.is_dir():
       @app.get("/{path:path}")
       async def serve_spa(path: str):
           """Serve static files or fall back to index.html for SPA routing."""
           file_path = DIST_DIR / path
           if file_path.is_file():
               return FileResponse(file_path)
           return FileResponse(DIST_DIR / "index.html")
   ```

CRITICAL ordering: The catch-all route MUST come AFTER all API router includes. API routes are matched first by FastAPI's declaration order. If the catch-all is registered before routers, /api/predict would return index.html.

The `if DIST_DIR.is_dir()` guard ensures local dev without a build still works (Vite proxy handles routing in dev).

Do NOT add a StaticFiles mount -- the catch-all route pattern handles both static assets and SPA fallback in one place, which is simpler and avoids mount ordering issues.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/Luis Conceição/OneDrive - HHS Host Hotel Systems/Desktop/tese" && test -f frontend/dist/index.html && python -c "
from pathlib import Path
import ast

main_py = Path('api/main.py').read_text()
tree = ast.parse(main_py)

# Verify FileResponse import exists
imports = [n for n in ast.walk(tree) if isinstance(n, ast.ImportFrom)]
has_file_response = any('FileResponse' in [a.name for a in n.names] for n in imports if n.module and 'fastapi' in n.module)
assert has_file_response, 'Missing FileResponse import'

# Verify catch-all comes after router includes
lines = main_py.split('\n')
last_router_line = max(i for i, l in enumerate(lines) if 'include_router' in l)
spa_line = next(i for i, l in enumerate(lines) if 'serve_spa' in l or 'path:path' in l)
assert spa_line > last_router_line, f'SPA catch-all (line {spa_line}) must come after last router include (line {last_router_line})'

print('SPA integration verified: FileResponse imported, catch-all after routers')
"</automated>
    <manual>Run `uvicorn api.main:app --port 8000` and visit http://localhost:8000 -- should see the React app. Visit http://localhost:8000/health -- should see JSON health response (not index.html).</manual>
  </verify>
  <done>frontend/dist/ exists with compiled assets. api/main.py serves SPA at root with catch-all AFTER all API routers. /health and /api/* still return JSON responses.</done>
</task>

<task type="auto">
  <name>Task 2: Validate production requirements and add .gitignore for dist</name>
  <files>requirements-prod.txt, .gitignore</files>
  <action>
1. Validate requirements-prod.txt does NOT contain training-only deps:
   - Must NOT contain: xgboost, lightgbm, catboost, optuna, pandas, matplotlib, seaborn, shap, tqdm
   - Must contain: fastapi, scikit-learn==1.5.0, joblib, numpy, python-multipart
   - The file already exists from Phase 2 Plan 03 -- verify it is correct, fix only if needed

2. Add frontend/dist/ to .gitignore (build artifacts should not be committed):
   - Add line: `frontend/dist/` to .gitignore
   - This keeps the repo clean; Render builds the frontend via buildCommand

3. Also add frontend/node_modules/ to .gitignore if not already present.

Do NOT modify the requirements-prod.txt unless it actually contains excluded packages. It was already created and verified in Phase 2 Plan 03.
  </action>
  <verify>
    <automated>cd "/mnt/c/Users/Luis Conceição/OneDrive - HHS Host Hotel Systems/Desktop/tese" && python -c "
req = open('requirements-prod.txt').read().lower()
# Check excluded packages are absent
for pkg in ['xgboost', 'lightgbm', 'catboost', 'optuna', 'pandas', 'matplotlib', 'seaborn', 'shap']:
    assert pkg not in req, f'Found excluded package: {pkg}'
# Check required packages are present
for pkg in ['fastapi', 'scikit-learn==1.5.0', 'joblib', 'numpy']:
    assert pkg in req, f'Missing required package: {pkg}'
print('requirements-prod.txt validated: no training deps, all inference deps present')
" && grep -q 'frontend/dist' .gitignore && echo '.gitignore has frontend/dist entry'</automated>
  </verify>
  <done>requirements-prod.txt contains only inference dependencies. .gitignore excludes frontend/dist/ and frontend/node_modules/.</done>
</task>

</tasks>

<verification>
1. frontend/dist/index.html exists and contains compiled React app
2. uvicorn api.main:app serves React app at / and API at /api/*
3. requirements-prod.txt has zero training-only dependencies
4. .gitignore excludes build artifacts
</verification>

<success_criteria>
- Running `uvicorn api.main:app --port 8000` and visiting http://localhost:8000 shows the React draft board
- Hitting http://localhost:8000/health returns JSON with model status
- Hitting http://localhost:8000/api/champions returns champion list JSON
- requirements-prod.txt installs in under 2 minutes without XGBoost/LightGBM/CatBoost
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-deployment/04-01-SUMMARY.md`
</output>
