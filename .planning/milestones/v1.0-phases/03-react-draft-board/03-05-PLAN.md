---
phase: 03-react-draft-board
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - frontend/src/store/draftStore.ts
  - frontend/src/components/SeriesTracker.tsx
  - frontend/src/components/DraftBoard.tsx
  - frontend/src/components/DraftControls.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - BOS-01
  - BOS-02
  - BOS-03

must_haves:
  truths:
    - "User can toggle between Single Game, BO3, and BO5 series formats"
    - "Series score is tracked and displayed across multiple games"
    - "Next Game button resets draft but preserves teams and series score"
    - "Series winner is declared when score threshold is reached"
    - "The complete draft board is visually cohesive and functional end-to-end"
  artifacts:
    - path: "frontend/src/components/SeriesTracker.tsx"
      provides: "BO3/BO5 toggle, score display, and series status"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/SeriesTracker.tsx"
      to: "frontend/src/store/draftStore.ts"
      via: "setSeriesFormat, recordGameResult, resetDraft"
      pattern: "recordGameResult"
    - from: "frontend/src/components/DraftControls.tsx"
      to: "frontend/src/store/draftStore.ts"
      via: "Next Game button calls resetDraft()"
      pattern: "resetDraft"
---

<objective>
Add best-of-series support (BO3/BO5) with score tracking, next-game flow, and a final visual polish checkpoint to verify the complete draft board end-to-end.

Purpose: Series tracking completes the feature set for the draft board. The checkpoint ensures the full UI is cohesive and functional before Phase 4 integration.
Output: A complete, polished React draft board supporting single games, BO3, and BO5 series with score tracking.
</objective>

<execution_context>
@/home/lvc/.claude/get-shit-done/workflows/execute-plan.md
@/home/lvc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-draft-board/03-RESEARCH.md
@.planning/phases/03-react-draft-board/03-CONTEXT.md
@.planning/phases/03-react-draft-board/03-03-SUMMARY.md
@.planning/phases/03-react-draft-board/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build series tracker component and wire next-game flow</name>
  <files>
    frontend/src/store/draftStore.ts
    frontend/src/components/SeriesTracker.tsx
    frontend/src/components/DraftBoard.tsx
    frontend/src/components/DraftControls.tsx
  </files>
  <action>
Create src/components/SeriesTracker.tsx:
- Not prominent by default (per CONTEXT.md: "secondary toggle, not prominent unless activated, defaults to single game")
- Shows a small dropdown or segmented control: "Single" | "BO3" | "BO5"
- When BO3 or BO5 is selected, expands to show:
  - Series score: "Blue 1 - 0 Red" with side-colored numbers
  - Game indicators (circles or dots): filled for completed games, outlined for remaining
  - Current game number: "Game 2 of 3"
- When a series winner is reached (blue or red hits 2 for BO3, 3 for BO5), show "Series Winner: [Team Name]" in gold text and disable further game entry
- Reads `seriesFormat`, `seriesScore`, `currentGame`, `blueTeam`, `redTeam` from store

Update store/draftStore.ts:
- `recordGameResult(winner: Side)`: increments `seriesScore[winner]`, increments `currentGame`, calls `resetDraft()` internally (clears picks/bans/roles, preserves teams and series)
- `setSeriesFormat(format)`: if changing format, reset series score to 0-0 and currentGame to 1 (but preserve draft state if in progress)
- Add `isSeriesComplete()`: returns true if either side has reached the win threshold
- `resetAll()` also resets series format back to 'single'

Update DraftControls.tsx:
- After a prediction is displayed, show "Record Result" section with two buttons: "Blue Wins" / "Red Wins" (only visible when seriesFormat is not 'single')
- Clicking records the result via `store.recordGameResult(side)`
- "Next Game" button (visible after recording result): resets draft for the next game
- If series is complete, replace Next Game with "New Series" button that calls `store.resetAll()`
- For single game mode: after prediction, only show "New Draft" (which is Reset Draft) and "Reset All"

Update DraftBoard.tsx:
- Place SeriesTracker in the header area near the ModeToggle (not dominant -- small, to the right or below the mode toggle)
- Ensure SeriesTracker is visually subtle when set to "Single" (just the dropdown, no score display)

Update App.tsx if needed:
- Ensure the overall layout is clean: header (title, mode toggle, series tracker), draft board (team panels + win prob), champion grid
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>Switch to BO3. Complete a draft, get prediction, click "Blue Wins". Draft should reset but teams and score stay. Score shows 1-0. Complete another draft, record result. If same team wins twice, series winner should be declared.</manual>
  </verify>
  <done>Series tracker toggles between Single/BO3/BO5, score tracks across games, Next Game resets draft but preserves teams and score, series winner declared at threshold</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete draft board</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Human verification of the complete React draft board. What was built: live draft mode, bulk entry mode, champion grid with search, team selectors, role assignment, win probability display, and best-of-series tracking. All requirements DRAFT-01 through DRAFT-07, LIVE-01 through LIVE-03, BULK-01 through BULK-02, and BOS-01 through BOS-03.
  </action>
  <verify>
    <manual>
    1. Start the FastAPI backend: `cd api && uvicorn main:app --reload`
    2. Start the Vite dev server: `cd frontend && npm run dev`
    3. Visit http://localhost:5173

    **Live Draft Mode (default):**
    a. Verify dark theme with correct colors (dark background, blue/red accents, gold highlights)
    b. Select a blue team from LCK and a red team from LPL using the team dropdowns
    c. Click champions one by one -- verify they fill ban slots first (alternating blue/red), then picks in the correct professional order
    d. Verify the phase label updates (Ban Phase 1, Pick Phase 1, Ban Phase 2, Pick Phase 2)
    e. Verify selected champions are greyed out in the grid
    f. Verify search filters the champion grid by name
    g. After all 20 selections, verify role assignment UI appears for both teams
    h. Assign all 10 roles (5 per team)
    i. Click "Get Prediction" -- verify win probability bar appears with blue/red percentages

    **Bulk Entry Mode:**
    j. Click "Quick Entry" tab
    k. Click any empty slot, then click a champion -- verify it fills
    l. Fill all 20 slots in any order
    m. Assign roles and get prediction

    **Best-of-Series:**
    n. Select BO3 from the series dropdown
    o. Complete a draft, get prediction, record a winner
    p. Verify score updates and draft resets for next game
    q. Complete the series and verify winner is declared

    4. Verify the entire page has a cohesive LoL-themed look (dark panels, gold accents, hextech-inspired)
    </manual>
  </verify>
  <done>User approves: all 15 phase requirements are visually and functionally verified, complete draft board is cohesive and production-ready</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds
2. All 15 phase requirements (DRAFT-01 through BOS-03) are implemented and functional
3. Live draft, bulk entry, role assignment, prediction, and series tracking all work end-to-end
4. Visual theme matches LoL-inspired dark design from CONTEXT.md
</verification>

<success_criteria>
- BO3/BO5 series tracking works across multiple games
- Next Game preserves teams and score while resetting draft
- Series winner declared at correct threshold
- All 15 phase requirements verified by human checkpoint
- Complete draft board is visually cohesive and production-ready for Phase 4 integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-draft-board/03-05-SUMMARY.md`
</output>
