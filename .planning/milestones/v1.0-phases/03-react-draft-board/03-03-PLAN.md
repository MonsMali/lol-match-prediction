---
phase: 03-react-draft-board
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/src/store/draftStore.ts
  - frontend/src/components/DraftBoard.tsx
  - frontend/src/components/TeamPanel.tsx
  - frontend/src/components/BanRow.tsx
  - frontend/src/components/PickSlot.tsx
  - frontend/src/components/ChampionGrid.tsx
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03

must_haves:
  truths:
    - "In live mode, clicking a champion places it in the correct slot following the 20-step professional draft order"
    - "The active slot advances automatically after each pick/ban"
    - "The current draft phase (Ban Phase 1, Pick Phase 1, etc.) is visually indicated"
    - "Already-selected champions are greyed out in the grid and cannot be selected again"
    - "Draft board visually highlights which team's turn it is and which slot is active"
  artifacts:
    - path: "frontend/src/store/draftStore.ts"
      provides: "selectChampion action with live-mode auto-advance using DRAFT_SEQUENCE"
      contains: "DRAFT_SEQUENCE"
    - path: "frontend/src/components/DraftBoard.tsx"
      provides: "Draft phase indicator and turn information"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/ChampionGrid.tsx"
      to: "frontend/src/store/draftStore.ts"
      via: "onSelect calls store.selectChampion()"
      pattern: "selectChampion"
    - from: "frontend/src/store/draftStore.ts"
      to: "DRAFT_SEQUENCE[currentStep]"
      via: "Live mode reads current step to determine target slot"
      pattern: "DRAFT_SEQUENCE\\[.*currentStep"
---

<objective>
Wire the step-by-step live draft mode so that clicking a champion in the grid places it in the correct slot following the professional 20-step draft order, with visual feedback showing whose turn it is and what phase the draft is in.

Purpose: Live draft mode is the default and primary interaction pattern. Users follow along with a broadcast, entering picks/bans one at a time as they happen.
Output: A fully functional live draft where clicking champions fills ban/pick slots in the correct professional order.
</objective>

<execution_context>
@/home/lvc/.claude/get-shit-done/workflows/execute-plan.md
@/home/lvc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-draft-board/03-RESEARCH.md
@.planning/phases/03-react-draft-board/03-01-SUMMARY.md
@.planning/phases/03-react-draft-board/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement live draft selection flow and visual turn indicators</name>
  <files>
    frontend/src/store/draftStore.ts
    frontend/src/components/DraftBoard.tsx
    frontend/src/components/TeamPanel.tsx
    frontend/src/components/BanRow.tsx
    frontend/src/components/PickSlot.tsx
    frontend/src/components/ChampionGrid.tsx
  </files>
  <action>
Update store/draftStore.ts `selectChampion` action for live mode:
- Read `currentStep` to get the current `DraftStep` from `DRAFT_SEQUENCE[currentStep]`
- If `currentStep >= 20`, do nothing (draft complete)
- Place the champion name into the correct array slot: e.g., if step says `{ team: 'blue', action: 'ban', slotIndex: 2 }`, set `blueBans[2] = championName`
- Verify the champion is not in `usedChampions()` before placing (reject duplicates silently)
- Auto-advance: increment `currentStep` by 1 after successful placement
- Add a derived getter `currentDraftStep()` that returns the current `DraftStep` or null if complete
- Add a derived getter `currentPhaseLabel()` that returns a human-readable label: "Ban Phase 1" (steps 0-5), "Pick Phase 1" (steps 6-11), "Ban Phase 2" (steps 12-15), "Pick Phase 2" (steps 16-19), "Draft Complete" (step 20+)
- Add `undoLastStep()` action: decrements currentStep and clears the slot that was just filled (for correction)

Update DraftBoard.tsx:
- Read `currentDraftStep()` and `currentPhaseLabel()` from store
- Display the phase label prominently between the mode toggle and the team panels (e.g., "Ban Phase 1 -- Blue Team's Turn")
- Show the team name whose turn it is, colored with the appropriate side color
- When draft is complete (step 20), show "Draft Complete -- Assign Roles" message

Update TeamPanel.tsx:
- Determine which slot is active for this side by checking if `currentDraftStep()?.team` matches this panel's side
- Pass `activeBanIndex` or `activePickIndex` to BanRow / PickSlot components

Update BanRow.tsx:
- Highlight the active ban slot with a pulsing border animation (side color)
- Non-active filled slots show champion portrait at reduced saturation
- Empty non-active slots are dim

Update PickSlot.tsx:
- Highlight the active pick slot with a glowing border (side color)
- Empty non-active slots show a subtle outline
- Filled slots display champion portrait

Update ChampionGrid.tsx:
- Ensure `onSelect` is only callable when there is an active slot (in live mode: `currentStep < 20`; in bulk mode: `activeSlot` is set)
- When no active slot, show a subtle message below the search bar: "Select a slot first" (bulk mode) or "Draft complete" (live mode, step >= 20)
- Consider adding a visual indicator on the grid showing whether the current action is a ban or pick (e.g., "Banning for Blue Team" or "Picking for Red Team" text above the grid)

All changes must preserve the existing component structure and props from Plan 02. Do not break the bulk mode path (bulk mode wiring happens in Plan 04).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>With backend running, visit localhost:5173. In live mode (default), click champions one by one. First 6 clicks should fill ban slots alternating blue/red. Next 6 fill picks in the B1-R1-R2-B2-B3-R3 pattern. Then 4 more bans (R-B-R-B), then 4 final picks (R-B-B-R). Each click should advance to the next step, and the grid should grey out used champions.</manual>
  </verify>
  <done>Live draft mode fully functional: clicking champions fills slots in correct 20-step order, active slot is highlighted, phase labels show current state, used champions are greyed out in the grid</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` succeeds
2. Live draft follows exact 20-step professional order
3. Active slot is visually highlighted with side-colored border
4. Phase label updates correctly through all 4 phases
5. Already-used champions cannot be selected again
6. Undo action reverses the last step
</verification>

<success_criteria>
- 20-step professional draft sequence works correctly in order
- Visual indicators show whose turn it is and current phase
- Used champions greyed out in grid (not hidden, grid stays stable)
- Active slot has clear visual highlight
- Draft stops accepting input after step 20
</success_criteria>

<output>
After completion, create `.planning/phases/03-react-draft-board/03-03-SUMMARY.md`
</output>
