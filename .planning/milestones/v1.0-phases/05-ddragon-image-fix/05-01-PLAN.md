---
phase: 05-ddragon-image-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useChampionLookup.ts
  - frontend/src/components/ChampionImage.tsx
  - frontend/src/components/PickSlot.tsx
  - frontend/src/components/BanRow.tsx
  - frontend/src/components/RoleAssignment.tsx
autonomous: true
requirements: [DRAFT-07, API-08]

must_haves:
  truths:
    - "All 12 special-name champions (Wukong, Kai'Sa, Nunu & Willump, Kha'Zix, Cho'Gath, Vel'Koz, Rek'Sai, Kog'Maw, Bel'Veth, K'Sante, LeBlanc, Renata Glasc) render correct portraits in pick slots, ban slots, and role assignment"
    - "A shimmer skeleton appears in the portrait area while champion images load"
    - "Champion portraits fade in with ~150ms opacity transition when loaded"
    - "When an image fails to load, a team-colored SVG silhouette appears immediately (no retry)"
    - "Champion name is visible immediately on selection regardless of image load state"
  artifacts:
    - path: "frontend/src/hooks/useChampionLookup.ts"
      provides: "Champion name-to-ChampionInfo lookup map"
      exports: ["useChampionLookup"]
    - path: "frontend/src/components/ChampionImage.tsx"
      provides: "Shared image component with shimmer, fade-in, and SVG fallback"
      exports: ["ChampionImage"]
    - path: "frontend/src/components/PickSlot.tsx"
      provides: "Pick slot using API image_url via lookup"
      contains: "useChampionLookup"
    - path: "frontend/src/components/BanRow.tsx"
      provides: "Ban row using API image_url via lookup"
      contains: "useChampionLookup"
    - path: "frontend/src/components/RoleAssignment.tsx"
      provides: "Role assignment using API image_url via lookup"
      contains: "useChampionLookup"
  key_links:
    - from: "frontend/src/hooks/useChampionLookup.ts"
      to: "frontend/src/api/champions.ts"
      via: "useChampions() query data"
      pattern: "useChampions"
    - from: "frontend/src/components/PickSlot.tsx"
      to: "frontend/src/components/ChampionImage.tsx"
      via: "ChampionImage component with resolved image_url"
      pattern: "ChampionImage"
    - from: "frontend/src/components/BanRow.tsx"
      to: "frontend/src/components/ChampionImage.tsx"
      via: "ChampionImage component with resolved image_url"
      pattern: "ChampionImage"
    - from: "frontend/src/components/RoleAssignment.tsx"
      to: "frontend/src/components/ChampionImage.tsx"
      via: "ChampionImage component with resolved image_url"
      pattern: "ChampionImage"
---

<objective>
Fix broken champion portrait URLs in PickSlot, BanRow, and RoleAssignment by replacing direct DDragon URL construction with API-provided image_url. Add polished loading states (shimmer, fade-in, team-colored SVG fallback).

Purpose: 12 champions with DDragon ID mismatches (e.g., Wukong=MonkeyKing) show broken images. The API already returns correct URLs; the frontend just needs to use them.
Output: All champion portraits render correctly with professional loading UX.
</objective>

<execution_context>
@/home/lvc/.claude/get-shit-done/workflows/execute-plan.md
@/home/lvc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ddragon-image-fix/05-RESEARCH.md

@frontend/src/components/ChampionIcon.tsx (reference pattern -- uses champion.image_url correctly)
@frontend/src/api/champions.ts (useChampions hook returning ChampionInfo[])
@frontend/src/types/index.ts (ChampionInfo type with image_url field)
@frontend/src/components/PickSlot.tsx (broken -- constructs DDRAGON_BASE/${champion}.png)
@frontend/src/components/BanRow.tsx (broken -- constructs DDRAGON_BASE/${ban}.png)
@frontend/src/components/RoleAssignment.tsx (broken -- constructs DDRAGON_BASE/${champion}.png)
@frontend/src/index.css (team color vars: --color-blue-team: #0088ff, --color-red-team: #ff3333)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useChampionLookup hook and ChampionImage component</name>
  <files>
    frontend/src/hooks/useChampionLookup.ts
    frontend/src/components/ChampionImage.tsx
  </files>
  <action>
    Create `frontend/src/hooks/useChampionLookup.ts`:
    - Export `useChampionLookup()` hook that returns `Map<string, ChampionInfo>`
    - Import `useChampions` from `../api/champions` and `ChampionInfo` from `../types`
    - Use `useMemo` to build a Map keyed by `champion.name` from the `useChampions()` data array
    - Return empty Map when data is undefined (loading state)

    Create `frontend/src/components/ChampionImage.tsx`:
    - Props: `src: string | undefined`, `alt: string`, `side: 'blue' | 'red'`, `className?: string`
    - Internal state: `status: 'loading' | 'loaded' | 'error'` starting at `'loading'`
    - Reset status to `'loading'` when `src` changes (useEffect on src) to handle Pitfall 3 from research
    - Render three layers in a relative container:
      1. Shimmer: Show when status is `'loading'` -- use `animate-pulse` on a `bg-panel-light` div covering the full area
      2. Error fallback: Show when status is `'error'` -- inline SVG with a `<rect>` background (#252540) and a head-and-shoulders silhouette (`<circle>` + `<ellipse>`) filled with team color at 0.4 opacity. Blue = `#0088ff`, Red = `#ff3333` (matching CSS vars)
      3. Image: Always render the `<img>` tag (hidden via opacity-0 until loaded). Use `transition-opacity duration-150` for fade-in. Set `opacity-100` when status is `'loaded'`, `opacity-0` otherwise. Wire `onLoad` to set status `'loaded'` and `onError` to set status `'error'`
    - If `src` is undefined/falsy, show shimmer only (no img tag rendered)
    - Do NOT retry on error (user locked decision)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify ChampionImage.tsx exports correctly and useChampionLookup.ts compiles</manual>
  </verify>
  <done>useChampionLookup hook returns a Map from champion name to ChampionInfo. ChampionImage renders shimmer during load, fades in on success, shows team-colored SVG silhouette on error.</done>
</task>

<task type="auto">
  <name>Task 2: Update PickSlot, BanRow, and RoleAssignment to use lookup and ChampionImage</name>
  <files>
    frontend/src/components/PickSlot.tsx
    frontend/src/components/BanRow.tsx
    frontend/src/components/RoleAssignment.tsx
  </files>
  <action>
    Update `frontend/src/components/PickSlot.tsx`:
    - Remove the `DDRAGON_BASE` constant
    - Import `useChampionLookup` from `../hooks/useChampionLookup` and `ChampionImage` from `./ChampionImage`
    - Call `useChampionLookup()` at the top of the component to get the lookup map
    - Resolve: `const championInfo = champion ? championLookup.get(champion) : undefined`
    - Replace the raw `<img src={DDRAGON_BASE/${champion}.png}>` with `<ChampionImage src={championInfo?.image_url} alt={champion} side={side} className="w-full h-full" />`
    - Keep the role badge overlay unchanged

    Update `frontend/src/components/BanRow.tsx`:
    - Remove the `DDRAGON_BASE` constant
    - Import `useChampionLookup` from `../hooks/useChampionLookup` and `ChampionImage` from `./ChampionImage`
    - Call `useChampionLookup()` at the top of the component
    - Inside the map: `const banInfo = ban ? championLookup.get(ban) : undefined`
    - Replace the raw `<img src={DDRAGON_BASE/${ban}.png}>` with `<ChampionImage src={banInfo?.image_url} alt={ban ?? ''} side={side} className="w-full h-full" />`
    - Keep the existing grayscale + "X" overlay for bans. Move the grayscale class to a wrapper div around ChampionImage so it applies to the loaded image. The "X" overlay span stays as-is on top.

    Update `frontend/src/components/RoleAssignment.tsx`:
    - Remove the `DDRAGON_BASE` constant
    - Import `useChampionLookup` from `../hooks/useChampionLookup` and `ChampionImage` from `./ChampionImage`
    - Call `useChampionLookup()` at the top of the component
    - Inside the map: `const championInfo = champion ? championLookup.get(champion) : undefined`
    - Replace the raw `<img src={DDRAGON_BASE/${champion}.png}>` with `<ChampionImage src={championInfo?.image_url} alt={champion} side={side} className="w-8 h-8" />`
    - Keep champion name text and role select dropdown unchanged

    CRITICAL: No component should contain `DDRAGON_BASE` or construct URLs from champion display names after this task. All image URLs must come from the API via the lookup map.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30 && grep -r "DDRAGON_BASE" src/components/PickSlot.tsx src/components/BanRow.tsx src/components/RoleAssignment.tsx; echo "grep exit: $?"</automated>
    <manual>Run `npm run dev`, complete a draft with Wukong and Kai'Sa as picks -- both portraits should render correctly. Ban Cho'Gath -- portrait should render with grayscale X overlay.</manual>
  </verify>
  <done>All three components use API-provided image_url via useChampionLookup. No DDRAGON_BASE constant remains in any of the three files. Wukong, Kai'Sa, and all other 12 special-name champions render correct portraits in picks, bans, and role assignment.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd frontend && npx tsc --noEmit` passes with zero errors
2. No DDRAGON_BASE in slot components: `grep -r "DDRAGON_BASE" frontend/src/components/{PickSlot,BanRow,RoleAssignment}.tsx` returns no matches
3. Build succeeds: `cd frontend && npm run build` completes without errors
4. All three components import and use useChampionLookup
</verification>

<success_criteria>
- All 12 special-name champions display correct portraits in PickSlot, BanRow, and RoleAssignment
- Shimmer animation shows while images load in all slot types
- Portrait fades in with ~150ms transition when loaded
- Failed images show team-colored SVG silhouette (no retry)
- Champion names appear immediately on selection regardless of image load state
- TypeScript compiles cleanly, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-ddragon-image-fix/05-01-SUMMARY.md`
</output>
